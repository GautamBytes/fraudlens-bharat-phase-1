# Screenshot Evidence

Use `final_*.png` for the final Phase 1 + Phase 2 report, presentation, and demo
fallback. These captures use synthetic fixtures and show the current text, OCR,
entity-graph, readiness, and API-documentation paths.

Files without the `final_` prefix are historical Phase 1 development captures.
They remain for traceability and must not be used as current test-count or model
accuracy evidence.
